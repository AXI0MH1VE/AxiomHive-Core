# Contributing

Edits must be deterministic and auditable.

- One logical change per PR.
- Update the recorded `SHA256: ...` line at file end.
- CI will block mismatches. Commit messages: `feat:`, `fix:`, `docs:`, `governance:`.
